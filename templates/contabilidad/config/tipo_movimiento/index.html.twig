{% extends 'contabilidad/config/index.html.twig' %}
{% block style %}
    <style>
        label {
            display: none;
        }
    </style>
{% endblock %}
{% block content %}
    <div class="container-fluid">

        {# header #}
        <div class="d-flex align-items-center">
            <h4 class="mr-auto display-4 mb-0 mr-auto title-table text-light">
                Tipos de Movimiento de Activos Fijos
            </h4>
        </div>
        {# end header #}

        {# list #}
        <table class="table-small-rows table table-hover text-light">
            <thead class="thead-dark">
            <th width="150px" class="text-left">Código</th>
            <th width="150px" class="text-left">Abreviatura</th>
            <th scope="col-2" class="text-left">Descripción</th>
            </thead>
            <tbody>
            {% for item in tipo_movimiento %}
                <tr>
                    <td style="font-weight:400;">{{ item.numero }}</td>
                    <td style="font-weight:400;">{{ item.abreviatura }}</td>
                    <td style="font-weight:400;">{{ item.descripcion }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {# end list #}
    </div>
{% endblock %}